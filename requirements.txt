# ─────────────────────────────────────────────────
# Product Recommendation Agent — Phase 3
# All packages are 100% free & open-source.
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────

# ── Phase 3.0: Production API ────────────────────
# FastAPI — high-performance async web framework
fastapi>=0.111.0
# Uvicorn — ASGI server with WebSocket & HTTP/2 support
uvicorn[standard]>=0.29.0
# SSE-Starlette — Server-Sent Events for streaming responses
sse-starlette>=1.8.0
# SlowAPI — IP-based rate limiting middleware (PRD §4)
slowapi>=0.1.9
# HTTPX — async HTTP client (used internally by FastAPI TestClient)
httpx>=0.27.0

# Core LangChain ecosystem
langchain>=0.2.0
langchain-core>=0.2.0
langgraph>=0.2.0
langchain-community>=0.2.0

# Google Gemini integration (free via Google AI Studio)
langchain-google-genai>=1.0.0

# ── Phase 2: Web Search ──────────────────────────
# Tavily Search API (free tier — optimised for LLM contexts)
tavily-python>=0.3.0

# ── Phase 2: Local Vector Store (Product Intelligence Vault) ──
# ChromaDB — persistent, disk-backed vector database (zero cloud cost)
chromadb>=0.5.0

# ── Phase 2: Local Embeddings ────────────────────
# Runs all-MiniLM-L6-v2 completely locally — no API calls, no cost
sentence-transformers>=3.0.0
langchain-huggingface>=0.1.0
langchain-chroma>=0.1.0

# ── Phase 2: Web Scraping ────────────────────────
beautifulsoup4>=4.12.0
requests>=2.31.0

# Structured output & validation
pydantic>=2.0.0

# Environment variable management
python-dotenv>=1.0.0

# LangSmith observability (free developer tier — opt-in via env vars)
langsmith>=0.1.0

